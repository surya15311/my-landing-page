/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["t4.ftcdn.net","fj-employer-blog.s3.amazonaws.com","fjwp.s3.amazonaws.com"],
    },
};

export default nextConfig;
